if SHIELD_CRUMPLE_LEFT

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "Crumple"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_CRUMPLE_LEFT || SHIELD_CRUMPLE_RIGHT

config ZMK_SPLIT
    default y

endif
